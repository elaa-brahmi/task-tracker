/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseTaskResponse } from '../../models/page-response-task-response';

export interface FindTasksByKeyword$Params {
  keyword: string;
  page?: number;
  size?: number;
}

export function findTasksByKeyword(http: HttpClient, rootUrl: string, params: FindTasksByKeyword$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseTaskResponse>> {
  const rb = new RequestBuilder(rootUrl, findTasksByKeyword.PATH, 'get');
  if (params) {
    rb.query('keyword', params.keyword, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseTaskResponse>;
    })
  );
}

findTasksByKeyword.PATH = '/tasks/search';
